# Contributing

Contributions are accepted under the MIT license. Create a focused branch,
add tests, run `npm run gate`, and open a pull request. Commits must not include
secrets, generated candidate artifacts, or changes that add data-plane tools,
arbitrary dispatch, SQL/SPARQL, database drivers, Compose rendering, or hosted
deployment.

One maintainer may merge after all required CI and security checks pass.
